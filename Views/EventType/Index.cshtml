
@{
    ViewBag.Title = "Event Types";
}

@{
    var eventTypes = ViewData["eventTypes"] as List<ComputerClub.Models.EventType>;
    var i = 1;
}

<div class="games">
    @Html.Partial("_LeftMenu")
    <div class="img-div">
        <h3>Усі типи івентів</h3>
        <div class="add-div">
            <a href='@Url.Action("Create", "EventType")'
               class="mybtn-warning">
                Додати тип івентів
            </a>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Назва</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in eventTypes)
                {
                <tr>
                    <td>@i @{i++;}</td>
                    <td><span>@item.Name</span></td>
                    <td>
                        <a href='@Url.Action("Edit", "EventType",
                                    new { EventTypeID = item.EventTypeID })'
                           class="mybtn-warning">
                            Edit
                        </a>
                    </td>
                    <td>
                        <form action='@Url.Action("Delete", "EventType",
                                         new { EventTypeID = item.EventTypeID })'
                              method="post" class="delete-form">
                            <input type="submit" class="mybtn-danger" value="Delete">
                        </form>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>
